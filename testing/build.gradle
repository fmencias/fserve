apply plugin: 'java'

description = 'A helper testing package of friendly serving HTTP library'

repositories {
  mavenCentral()
}

javadoc {
  failOnError = false
}

dependencies {
  compile project(':fserve')
}
